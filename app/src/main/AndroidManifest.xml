<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appmpsp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mpsplogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mpsplogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.DetranActivity"></activity>
        <activity android:name=".view.CadespView" />
        <activity android:name=".view.CadespActivity" />
        <activity android:name=".view.ArpenspView" />
        <activity android:name=".view.ArpenspActivity" />
        <activity android:name=".view.JucespView" />
        <activity android:name=".view.JucespActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>